import { IconButton, IconButtonProps } from '@mui/material';
import React from 'react';

function IconButtonAttom (props: IconButtonProps) {
  return <IconButton {...props} />;
};
export default IconButtonAttom;