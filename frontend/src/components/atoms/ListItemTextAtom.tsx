import { ListItemText, ListItemTextProps } from '@mui/material';
import React from 'react';

function ListItemTextAtom(props: ListItemTextProps) {
  return <ListItemText {...props} />
}

export default ListItemTextAtom;